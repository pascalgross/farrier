# Copy to .env beside compose.yaml and fill in. Compose reads it automatically.
#
#   cp .env.example .env
#
# Every value without a default below is required, and `docker compose up` says which one is missing
# rather than starting something half-configured. .env is gitignored; treat it as you would the database
# it grants access to.
#
# Generate the secrets rather than inventing them:
#
#   openssl rand -hex 32

# --- Credentials the database is created with, on the first start only -------------------------------
#
# Changing one of these afterwards changes nothing: the roles already exist, and the init scripts do not
# run again. Change a password in SQL — ALTER ROLE farrier PASSWORD '…' — and here, in that order.

# The cluster's bootstrap superuser. farrier-server never connects as this role, and would refuse to if
# you pointed it at one: a superuser is exempt from every row-level security policy in the schema, which
# is what isolates fleets from one another. See docs/SECURITY.md §5.
POSTGRES_SUPERUSER_PASSWORD=

# The ordinary role the control plane connects as, and the database it owns.
FARRIER_DB_USER=farrier
FARRIER_DB_PASSWORD=
FARRIER_DB_NAME=farrier

# The role a standby streams as. Created on the first start whether or not a standby ever exists,
# because adding it later means editing pg_hba.conf on a primary that is already serving — at the
# moment somebody is trying to build a replica under time pressure.
FARRIER_REPLICATION_USER=replicator
FARRIER_REPLICATION_PASSWORD=
# Where a standby may connect from, in pg_hba.conf terms. samenet is any subnet this container is
# directly attached to, which inside Compose is this project's network. A standby on another host needs
# a CIDR here, and a network you would be willing to send a copy of the database over.
FARRIER_REPLICATION_HBA=samenet

# --- The control plane -------------------------------------------------------------------------------

# The operator credential for this installation's fleet. Everything the interface and /api/v1 do is
# behind it. Without it set, the server generates one on every start and prints it, which is fine for a
# demonstration and useless for anything that has to survive a restart.
FARRIER_ADMIN_TOKEN=

# Tenant administration: creating and configuring fleets, and reaching no fleet's hosts or jobs. Leave
# it empty unless you run Farrier for other people — an empty value means nothing can create a second
# fleet, which is the correct configuration for almost every installation.
FARRIER_PLATFORM_TOKEN=

# The fleet FARRIER_ADMIN_TOKEN acts in. Created on first start if it does not exist.
FARRIER_TENANT=default

# --- Where it listens --------------------------------------------------------------------------------

# Only used by compose.yaml's published port, which compose.traefik.yaml removes. The default binds the
# port on every address of the host; 127.0.0.1 is the right answer when something else on the machine is
# the only thing that should reach it.
FARRIER_PUBLISH_ADDR=0.0.0.0
FARRIER_PORT=8443

# --- Traefik, if you use compose.traefik.yaml --------------------------------------------------------

# The name agents and operators use. It is matched as SNI on a TCP router: Traefik routes the connection
# and farrier-server terminates it, because a terminating proxy would end the connection that carries an
# agent's client certificate.
FARRIER_HOSTNAME=
FARRIER_TRAEFIK_ENTRYPOINT=websecure

# --- The image ---------------------------------------------------------------------------------------

# Left as it is, `docker compose up` builds the image from this repository. Set FARRIER_IMAGE to a
# published one to deploy without a checkout.
# FARRIER_IMAGE=farrier-server:local
# FARRIER_VERSION=0.0.0-compose

# --- A standby, if you use compose.standby.yaml ------------------------------------------------------

# The replication slot the standby holds on the primary. Rebuilding a standby means dropping this slot
# on the primary first, or naming a new one here.
FARRIER_STANDBY_SLOT=standby1
